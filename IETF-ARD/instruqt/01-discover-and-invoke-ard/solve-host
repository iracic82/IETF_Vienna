#!/bin/bash
# Auto-solve: drive the assistant non-interactively to complete the
# challenge. Useful for `instruqt track test`.
set -euo pipefail

docker exec strands-agent bash -c "echo 'Is 185.220.101.45 malicious?' | python /app/agent.py" || true
exit 0
