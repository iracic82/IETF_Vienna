#!/bin/bash
# No-op on per-challenge transition. C2 + C3 depend on the sandbox
# stack started in C1 setup-host — we must NOT docker-compose-down
# between challenges. Containers go away naturally when the sandbox
# VM is destroyed at track end. DNS records are cleaned by
# track_scripts/cleanup-host.
exit 0
